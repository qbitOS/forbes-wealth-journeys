# JAX / Colossus Distributed Training

JAX MoE + multi-host patterns for xAI Colossus supercluster.

Preferred stack for Grok/Dojo/Colossus training pipelines.

```bash
python examples/jax-colossus/train_moe.py --config examples/jax-colossus/configs/moe.yaml
```

See `docs/colossus-cluster-setup.md` and `scripts/colossus/colossus-job.sh`.
