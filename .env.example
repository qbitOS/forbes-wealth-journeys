# Copy to .env and fill in values (never commit .env)

# xAI / Grok API
XAI_API_KEY=
GROK_MODEL=grok-4

# GitHub (for connector workflows)
GITHUB_TOKEN=

# Colossus / cluster
COLOSSUS_CLUSTER=
SLURM_PARTITION=gpu

# DVC remote (optional)
DVC_REMOTE=s3://your-bucket/dvc-store
