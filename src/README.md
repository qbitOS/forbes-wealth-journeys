# src/

Core library code. Mirror layout in `tests/`.

Entry points are in `scripts/`; keep reusable logic here.

CUDA kernel starters live under `kernels/`; shared helpers in `cuda/`. Match CUDA 12.4 per `Dockerfiles/Dockerfile.colossus`.
