# configs/

YAML/JSON configs for training, Colossus scaling, and domain examples.

- `default.yaml` — local/single-node training
- `colossus.yaml` — multi-node GPU scaling (JAX multi-host)

Domain configs live under `examples/*/configs/`.
